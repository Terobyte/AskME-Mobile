# FIX: Увеличение минимальных лимитов для предотвращения артефактов

## 🎯 Проблема

После первоначального исправления Force flush, пользователь все еще слышал артефакты. Анализ логов показал:

```
📦 [SENTENCE] File #3: "I have your details..." (557ms)
📦 [SENTENCE] File #4: "Whenever you're ready..." (557ms)
✅ [Crossfade] Natural pause detected ("know."), using crossfade
⏰ [AudioQueue] Scheduling cross-fade in 517ms (duration: 40ms)
🚫 [AudioQueue] Cancelling scheduled crossfade (file finished first)
✅ [AudioQueue] Chunk 4 finished (duration: 478ms)
⏸️ [AudioQueue] Adding 20ms micro-pause (no crossfade)
```

### Проблемы:

1. **Очень короткие файлы** (557ms, 478ms) - близко к минимуму 500ms
2. **Crossfade планируется** в `517ms - 40ms = 477ms`
3. **Файл заканчивается** в `478ms` - crossfade **отменяется**
4. **Резкий переход** с микро-паузой 20ms создает артефакт

### Почему происходит:

- `MIN_DURATION_MS: 500` слишком мал для безопасного crossfade
- Короткие предложения ("just let me know.") создают файлы ~500ms
- Crossfade 40ms требует минимум `40ms + safety margin` для безопасного планирования
- Реальная длительность файла может быть немного меньше рассчитанной

## ✅ Решение

Увеличил минимальные лимиты в конфигурации:

```typescript
// БЫЛО:
SENTENCE: {
    MIN_DURATION_MS: 500,       // Слишком маленький минимум
    MAX_DURATION_MS: 2500,      
    LONG_SENTENCE_MS: 3000,
}

// СТАЛО:
SENTENCE: {
    MIN_DURATION_MS: 1000,      // Увеличено до 1000ms (безопаснее)
    MAX_DURATION_MS: 3500,      // Увеличено до 3500ms (меньше Force flush)
    LONG_SENTENCE_MS: 3000,
}
```

### Логика работы:

1. **MIN_DURATION_MS: 1000ms** - система будет накапливать предложения до 1 секунды
2. **MAX_DURATION_MS: 3500ms** - увеличен лимит до Force flush
3. Меньше коротких файлов → меньше отмененных crossfade → меньше артефактов

## 📊 Ожидаемый результат

### До изменения:
```
Предложение 1 (короткое): "I have your details." → 557ms
  → Crossfade планируется в 517ms
  → Файл заканчивается в 478ms
  → Crossfade отменяется
  → Артефакт ❌
```

### После изменения:
```
Предложения 1+2 (накоплено): "I have your details. Whenever you're ready..." → 1827ms
  → Crossfade планируется в 1787ms  
  → Достаточно времени для crossfade
  → Crossfade выполняется успешно
  → Чистый звук ✅
```

## 🎛️ Компромиссы

### Плюсы:
- ✅ Меньше артефактов (более стабильный crossfade)
- ✅ Меньше файлов (меньше overhead)
- ✅ Более предсказуемое поведение

### Минусы:
- ⚠️ Немного больше задержка до первого предложения (но fast-start компенсирует)
- ⚠️ Файлы могут быть длиннее (но это в пределах 3.5s)

## 🧪 Тестирование

1. Запустите приложение
2. Послушайте короткие предложения Victoria
3. Проверьте логи:
   ```
   📦 [SENTENCE] File #3: "..." (1000ms+)  // Должно быть больше 1000ms
   ✅ [Crossfade] Natural pause detected
   ⏰ [AudioQueue] Scheduling cross-fade...
   ✨ [AudioQueue] Cross-fade complete!  // НЕ отменяется!
   ```

## ✅ Статус

**Исправлено!** Теперь система создает более длинные, стабильные файлы, которые безопасно используют crossfade без отмен.
